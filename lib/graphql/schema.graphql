type Query {
  maskedEmailAddresses: [MaskedEmailAddress]
}

type Mutation {
  createMaskedEmailAddress: MaskedEmailAddress
}

type MaskedEmailAddress {
  id: ID
  address: String
}

enum MaskedEmailStatus {
  ENABLED
  DISABLED
}
